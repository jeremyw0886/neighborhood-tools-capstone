'use strict';
const CSRF_TOKEN=document.querySelector('meta[name="csrf-token"]')?.content??'';
const DEFAULT_TIMEOUT=10_000;
const TOAST_DURATION=5_000;
const ntFetch=async(url,opts={})=>{
const controller=new AbortController();
const timeout=opts.timeout??DEFAULT_TIMEOUT;
const timer=setTimeout(()=>controller.abort(),timeout);
if(opts.signal){
opts.signal.addEventListener('abort',()=>controller.abort(),{once:true});}
const headers=new Headers(opts.headers);
headers.set('X-CSRF-Token',CSRF_TOKEN);
headers.set('X-Requested-With','XMLHttpRequest');
if(!headers.has('Accept'))headers.set('Accept','application/json');
if(opts.body&&!(opts.body instanceof FormData)){
headers.set('Content-Type','application/json');}
try{
const response=await fetch(url,{...opts,
headers,
credentials:'same-origin',
signal:controller.signal,});
if(response.redirected&&new URL(response.url).pathname==='/login'){
toastManager.show('Your session has expired. Redirecting to login\u2026','error');
setTimeout(()=>{window.location.href='/login';},1500);
return Promise.reject(new Error('Session expired'));}
if(!response.ok){
switch(response.status){
case 401:
window.location.href='/login';
return Promise.reject(new Error('Unauthorized'));
case 403:
toastManager.show('Access denied.','error');
break;
case 422:
return response;
case 429:
toastManager.show('Too many requests, try again shortly.','error');
break;
default:
if(response.status>=500){
toastManager.show('Something went wrong.','error');}}}
return response;}finally{
clearTimeout(timer);}};
class ToastManager{
#container=null;#getContainer(){
if(this.#container)return this.#container;
this.#container=document.createElement('div');
this.#container.id='toast-container';
this.#container.setAttribute('aria-live','polite');
document.body.appendChild(this.#container);
return this.#container;}
show(message,type='info',duration=TOAST_DURATION){
const container=this.#getContainer();
const el=document.createElement('div');
el.setAttribute('role','status');
el.setAttribute('data-flash',type);
const iconMap={success:'fa-circle-check',error:'fa-circle-exclamation',info:'fa-circle-info'};
const icon=document.createElement('i');
icon.className=`fa-solid ${iconMap[type] ?? iconMap.info}`;
icon.setAttribute('aria-hidden','true');
const text=document.createElement('span');
text.textContent=message;
const dismiss=document.createElement('button');
dismiss.type='button';
dismiss.className='toast-dismiss';
dismiss.setAttribute('aria-label','Dismiss notification');
const dismissIcon=document.createElement('i');
dismissIcon.className='fa-solid fa-xmark';
dismissIcon.setAttribute('aria-hidden','true');
dismiss.appendChild(dismissIcon);
dismiss.addEventListener('click',()=>this.#remove(el));
el.append(icon,text,dismiss);
container.appendChild(el);
if(duration>0){
setTimeout(()=>this.#remove(el),duration);}}#remove(el){
if(!el.parentNode)return;
if(matchMedia('(prefers-reduced-motion: reduce)').matches){
el.remove();
return;}
el.classList.add('toast-exit');
el.addEventListener('animationend',()=>el.remove(),{once:true});}
migrateFlashMessages(){
const flashes=document.querySelectorAll('[data-flash]');
if(flashes.length===0)return;
const container=this.#getContainer();
for(const flash of flashes){
if(flash.closest('#toast-container'))continue;
flash.remove();
container.appendChild(flash);
const dismiss=document.createElement('button');
dismiss.type='button';
dismiss.className='toast-dismiss';
dismiss.setAttribute('aria-label','Dismiss notification');
const dismissIcon=document.createElement('i');
dismissIcon.className='fa-solid fa-xmark';
dismissIcon.setAttribute('aria-hidden','true');
dismiss.appendChild(dismissIcon);
dismiss.addEventListener('click',()=>this.#remove(flash));
flash.appendChild(dismiss);
setTimeout(()=>this.#remove(flash),TOAST_DURATION);}}}
const toastManager=new ToastManager();
const ntDynamicStyleEl=document.getElementById('nt-dynamic-styles');
class StyleManager{#sheet=ntDynamicStyleEl.sheet;#ruleMap=new Map();
setRule(key,selector,declarations){
this.removeRule(key);
const index=this.#sheet.insertRule(`${selector}{${declarations}}`,this.#sheet.cssRules.length);
this.#ruleMap.set(key,index);}
removeRule(key){
if(!this.#ruleMap.has(key))return;
const staleIndex=this.#ruleMap.get(key);
this.#sheet.deleteRule(staleIndex);
this.#ruleMap.delete(key);
for(const[k,v]of this.#ruleMap){
if(v>staleIndex)this.#ruleMap.set(k,v-1);}}
applyFocalPoints(root=document){
const imgs=(root.matches?.('img')?[root]:root.querySelectorAll('img'));
for(const img of imgs){
if(!img.id)img.id=`fp-${crypto.randomUUID().slice(0, 8)}`;
const key=`fp-${img.id}`;
const fx=img.dataset.focalX;
const fy=img.dataset.focalY;
if(fx&&fy){
this.setRule(key,`#${CSS.escape(img.id)}`,`object-position:${fx}% ${fy}%`);}else{
this.removeRule(key);}}}}
const styleManager=new StyleManager();
class FocusManager{
static#FOCUSABLE=['a[href]','button:not([disabled])','input:not([disabled]):not([type="hidden"])','select:not([disabled])','textarea:not([disabled])','[tabindex]:not([tabindex="-1"])',].join(',');
#announceRegion=null;
trap(container){
const handler=(e)=>{
if(e.key!=='Tab')return;
const focusable=[...container.querySelectorAll(FocusManager.#FOCUSABLE)]
.filter((el)=>el.offsetParent!==null);
if(focusable.length===0)return;
const first=focusable[0];
const last=focusable[focusable.length-1];
if(e.shiftKey&&document.activeElement===first){
e.preventDefault();
last.focus();}else if(!e.shiftKey&&document.activeElement===last){
e.preventDefault();
first.focus();}};
container.addEventListener('keydown',handler);
return()=>container.removeEventListener('keydown',handler);}
announce(message,priority='polite'){
if(!this.#announceRegion){
this.#announceRegion=document.createElement('div');
this.#announceRegion.className='visually-hidden';
this.#announceRegion.setAttribute('aria-live','polite');
this.#announceRegion.setAttribute('aria-atomic','true');
document.body.appendChild(this.#announceRegion);}
this.#announceRegion.setAttribute('aria-live',priority);
this.#announceRegion.textContent='';
requestAnimationFrame(()=>{
this.#announceRegion.textContent=message;});}}
const focusManager=new FocusManager();
class ConfirmDialog{
show(message,confirmLabel='Delete'){
return new Promise((resolve)=>{
const dialog=document.createElement('dialog');
dialog.setAttribute('aria-label','Confirmation');
dialog.dataset.confirm='';
const p=document.createElement('p');
p.textContent=message;
const footer=document.createElement('footer');
const cancelBtn=document.createElement('button');
cancelBtn.type='button';
cancelBtn.textContent='Cancel';
const confirmBtn=document.createElement('button');
confirmBtn.type='button';
confirmBtn.textContent=confirmLabel;
confirmBtn.setAttribute('data-intent','danger');
confirmBtn.setAttribute('autofocus','');
footer.append(cancelBtn,confirmBtn);
dialog.append(p,footer);
document.body.appendChild(dialog);
const cleanup=(result)=>{
dialog.close();
dialog.remove();
resolve(result);};
cancelBtn.addEventListener('click',()=>cleanup(false));
confirmBtn.addEventListener('click',()=>cleanup(true));
dialog.addEventListener('cancel',()=>cleanup(false));
dialog.showModal();});}}
const confirmDialog=new ConfirmDialog();
class DecisionDialog{
#autoTimer=null;
show({message,detail,nextUrl,nextLabel,stayLabel}){
const dialog=document.createElement('dialog');
dialog.dataset.decision='';
dialog.setAttribute('aria-label','Action completed');
const icon=document.createElement('i');
icon.className='fa-solid fa-circle-check';
icon.setAttribute('aria-hidden','true');
const p=document.createElement('p');
p.textContent=message;
dialog.append(icon,p);
if(detail){
const detailP=document.createElement('p');
detailP.textContent=detail;
detailP.dataset.detail='';
dialog.appendChild(detailP);}
const footer=document.createElement('footer');
const isSamePage=new URL(nextUrl,location.origin).pathname===location.pathname;
const cleanup=()=>{
if(this.#autoTimer)clearTimeout(this.#autoTimer);
dialog.close();
dialog.remove();};
if(isSamePage){
dialog.dataset.autoDismiss='';
const okBtn=document.createElement('button');
okBtn.type='button';
okBtn.textContent='OK';
okBtn.setAttribute('autofocus','');
okBtn.addEventListener('click',cleanup);
footer.appendChild(okBtn);
this.#autoTimer=setTimeout(cleanup,6_000);}else{
const nextBtn=document.createElement('a');
nextBtn.href=nextUrl;
nextBtn.textContent=nextLabel;
nextBtn.setAttribute('role','button');
nextBtn.setAttribute('autofocus','');
const stayBtn=document.createElement('button');
stayBtn.type='button';
stayBtn.textContent=stayLabel;
stayBtn.addEventListener('click',cleanup);
footer.append(nextBtn,stayBtn);}
dialog.appendChild(footer);
dialog.addEventListener('cancel',cleanup);
document.body.appendChild(dialog);
dialog.showModal();
NT.focus.announce(message);}}
const decisionDialog=new DecisionDialog();
class AutosuggestController{
#input;
#form;
#dropdown;#labelKey;#minLength;#limit;#onSelect;#renderItem;#fetchUrl;#fetchController=null;#debounceTimer=null;#activeIndex=-1;#currentItems=[];#abortController=new AbortController();
constructor(config){
this.#input=document.getElementById(config.inputId);
this.#form=this.#input.closest('form');
this.#minLength=config.minLength??2;
this.#limit=config.limit??5;
this.#labelKey=config.labelKey??null;
this.#onSelect=config.onSelect??null;
this.#renderItem=config.renderItem??null;
this.#fetchUrl=config.fetchUrl;
const listboxId=`${config.inputId}-suggestions`;
this.#dropdown=document.createElement('ul');
this.#dropdown.setAttribute('role','listbox');
this.#dropdown.id=listboxId;
this.#dropdown.setAttribute('aria-label','Search suggestions');
this.#dropdown.hidden=true;
(this.#input.parentElement??this.#form).appendChild(this.#dropdown);
this.#input.setAttribute('role','combobox');
this.#input.setAttribute('aria-autocomplete','list');
this.#input.setAttribute('aria-expanded','false');
this.#input.setAttribute('aria-controls',listboxId);
const{signal}=this.#abortController;
this.#input.addEventListener('input',this.#handleInput,{signal});
this.#input.addEventListener('blur',this.#handleBlur,{signal});
this.#input.addEventListener('keydown',this.#handleKeydown,{signal});}
static create(config){
const input=document.getElementById(config.inputId);
if(!input||!input.closest('form'))return null;
return new AutosuggestController(config);}
destroy(){
this.#abortController.abort();
clearTimeout(this.#debounceTimer);
this.#fetchController?.abort();
this.#dropdown.remove();
this.#input.removeAttribute('role');
this.#input.removeAttribute('aria-autocomplete');
this.#input.removeAttribute('aria-expanded');
this.#input.removeAttribute('aria-controls');
this.#input.removeAttribute('aria-activedescendant');}#label(item){
return this.#labelKey?item[this.#labelKey]:item;}#showDropdown(){
this.#dropdown.hidden=false;
this.#input.setAttribute('aria-expanded','true');}#hideDropdown(){
this.#dropdown.hidden=true;
this.#input.setAttribute('aria-expanded','false');
this.#setActiveIndex(-1);}#setActiveIndex(index){
const options=this.#dropdown.querySelectorAll('[role="option"]');
for(const opt of options){
opt.removeAttribute('aria-selected');}
this.#activeIndex=index;
if(index>=0&&index<options.length){
options[index].setAttribute('aria-selected','true');
this.#input.setAttribute('aria-activedescendant',options[index].id);
options[index].scrollIntoView({block:'nearest'});}else{
this.#input.removeAttribute('aria-activedescendant');}}#selectOption(item){
this.#hideDropdown();
if(this.#onSelect){
this.#onSelect(item,this.#form);}else{
this.#input.value=this.#label(item);
this.#form.requestSubmit();}}#renderSuggestions(items){
if(!items.length){
this.#hideDropdown();
return;}
this.#currentItems=items;
const listboxId=this.#dropdown.id;
this.#dropdown.replaceChildren(...items.map((item,i)=>{
const li=document.createElement('li');
li.setAttribute('role','option');
li.id=`${listboxId}-${i}`;
if(this.#renderItem){
this.#renderItem(li,item);}else{
li.textContent=this.#label(item);}
li.addEventListener('mousedown',(e)=>{
e.preventDefault();
this.#selectOption(item);});
return li;})
);
this.#activeIndex=-1;
this.#showDropdown();}
async#fetchSuggestions(query){
this.#fetchController?.abort();
this.#fetchController=new AbortController();
try{
const res=await ntFetch(this.#fetchUrl(query),{
signal:AbortSignal.any([
this.#fetchController.signal,
AbortSignal.timeout(5000),]),});
if(!res.ok)return;
const items=(await res.json()).slice(0,this.#limit);
this.#renderSuggestions(items);}catch(err){
if(err.name!=='AbortError')this.#hideDropdown();}}#handleInput=()=>{
const q=this.#input.value.trim();
clearTimeout(this.#debounceTimer);
if(q.length<this.#minLength){
this.#fetchController?.abort();
this.#hideDropdown();
return;}
this.#debounceTimer=setTimeout(()=>this.#fetchSuggestions(q),150);};#handleBlur=()=>{
setTimeout(()=>this.#hideDropdown(),150);};#handleKeydown=(e)=>{
const options=this.#dropdown.querySelectorAll('[role="option"]');
const count=options.length;
if(e.key==='ArrowDown'){
e.preventDefault();
if(this.#dropdown.hidden||!count)return;
this.#setActiveIndex(this.#activeIndex<count-1?this.#activeIndex+1:0);}else if(e.key==='ArrowUp'){
e.preventDefault();
if(this.#dropdown.hidden||!count)return;
this.#setActiveIndex(this.#activeIndex>0?this.#activeIndex-1:count-1);}else if(e.key==='Enter'&&this.#activeIndex>=0&&this.#activeIndex<count){
e.preventDefault();
this.#selectOption(this.#currentItems[this.#activeIndex]);}else if(e.key==='Escape'&&!this.#dropdown.hidden){
e.preventDefault();
this.#hideDropdown();}};}
class ScrollToTop{
static#instance=null;
static#HIDE_THRESHOLD=120;
static#MAX_SHOW_THRESHOLD=420;
static#SHOW_VIEWPORT_FACTOR=0.45;
#btn;#abortController=new AbortController();#rafId=0;#lastProgress=-1;
constructor(main){
this.#btn=document.createElement('button');
this.#btn.id='scroll-top';
this.#btn.type='button';
this.#btn.setAttribute('aria-label','Scroll to top');
document.body.appendChild(this.#btn);
const{signal}=this.#abortController;
this.#btn.addEventListener('click',this.#handleClick,{signal});
document.addEventListener('dashboard:content-swapped',this.#handleContentSwap,{signal});
window.addEventListener('scroll',this.#handleScroll,{passive:true,signal});
window.addEventListener('resize',this.#handleResize,{passive:true,signal});
this.#rafId=requestAnimationFrame(()=>{
this.#updateVisibility();
this.#updateProgress();});}
static init(){
if(ScrollToTop.#instance)return ScrollToTop.#instance;
const main=document.getElementById('main-content');
if(!main)return null;
return(ScrollToTop.#instance=new ScrollToTop(main));}
destroy(){
this.#abortController.abort();
cancelAnimationFrame(this.#rafId);
this.#btn.remove();
styleManager.removeRule('scroll-progress');
ScrollToTop.#instance=null;}
static#showThreshold(){
return Math.min(
ScrollToTop.#MAX_SHOW_THRESHOLD,
Math.round(window.innerHeight*ScrollToTop.#SHOW_VIEWPORT_FACTOR)
);}#updateProgress(){
const el=document.documentElement;
const scrollable=el.scrollHeight-el.clientHeight;
const raw=scrollable>0?Math.min(el.scrollTop/scrollable,1):0;
const rounded=Math.round(raw*100);
if(rounded!==this.#lastProgress){
this.#lastProgress=rounded;
styleManager.setRule('scroll-progress','#scroll-top',
`--progress:${raw};--progress-int:${rounded}`);}}#updateVisibility(){
const el=document.documentElement;
const scrollable=el.scrollHeight>el.clientHeight+1;
const scrollTop=el.scrollTop;
if(!scrollable||scrollTop<=ScrollToTop.#HIDE_THRESHOLD){
this.#btn.removeAttribute('data-visible');
return;}
if(scrollTop<ScrollToTop.#showThreshold())return;
this.#btn.setAttribute('data-animated','');
this.#btn.setAttribute('data-visible','');}#handleScroll=()=>{
cancelAnimationFrame(this.#rafId);
this.#rafId=requestAnimationFrame(()=>{
this.#updateVisibility();
this.#updateProgress();});};#handleClick=()=>{
window.scrollTo({top:0,behavior:'smooth'});};#handleResize=()=>{
cancelAnimationFrame(this.#rafId);
this.#rafId=requestAnimationFrame(()=>this.#updateVisibility());};#handleContentSwap=()=>{
this.#btn.removeAttribute('data-visible');
cancelAnimationFrame(this.#rafId);
this.#rafId=requestAnimationFrame(()=>{
this.#updateVisibility();
this.#updateProgress();});};}
class LazyFadeIn{
static#instance=null;
#mutationObserver;
constructor(){
for(const img of document.querySelectorAll('img[loading="lazy"]')){
LazyFadeIn.#observeImage(img);}
this.#mutationObserver=new MutationObserver((mutations)=>{
for(const mutation of mutations){
for(const node of mutation.addedNodes){
if(node.nodeType!==Node.ELEMENT_NODE)continue;
if(node.matches('img[loading="lazy"]')){
LazyFadeIn.#observeImage(node);}else{
for(const img of node.querySelectorAll('img[loading="lazy"]')){
LazyFadeIn.#observeImage(img);}}}}});
this.#mutationObserver.observe(document.body,{childList:true,subtree:true});}
static init(){
if(LazyFadeIn.#instance)return LazyFadeIn.#instance;
if(matchMedia('(prefers-reduced-motion: reduce)').matches)return null;
return(LazyFadeIn.#instance=new LazyFadeIn());}
destroy(){
this.#mutationObserver.disconnect();
LazyFadeIn.#instance=null;}
static#observeImage(img){
if(img.dataset.loaded!==undefined)return;
if(img.complete){
img.dataset.loaded='';}else{
img.addEventListener('load',()=>{img.dataset.loaded='';},{once:true});
img.addEventListener('error',()=>{img.dataset.loaded='';},{once:true});}}}
const errorIdFor=(field)=>
`${(field.id || field.name).replace(/_/g, '-')}-error`;
const validationMessage=(field)=>{
const v=field.validity;
if(v.valueMissing){
return field.tagName==='SELECT'?'Please select an option.':'This field is required.';}
if(v.typeMismatch&&field.type==='email')return'Please enter a valid email address.';
if(v.typeMismatch)return'Please enter a valid value.';
if(v.tooShort)return`Please enter at least ${field.minLength} characters.`;
if(v.tooLong)return`Please enter no more than ${field.maxLength} characters.`;
if(v.patternMismatch)return field.title||'Please match the requested format.';
if(v.rangeUnderflow)return`Value must be at least ${field.min}.`;
if(v.rangeOverflow)return`Value must be no more than ${field.max}.`;
if(v.stepMismatch)return'Please enter a valid value.';
if(v.badInput)return'Please enter a valid value.';
return'This field is invalid.';};
const showFieldError=(field)=>{
const id=errorIdFor(field);
if(document.getElementById(id))return;
const tag=field.closest('.auth-card .form-group')?'span':'p';
const el=document.createElement(tag);
el.id=id;
el.setAttribute('role','alert');
el.textContent=validationMessage(field);
field.setAttribute('aria-invalid','true');
const describedBy=field.getAttribute('aria-describedby')??'';
if(!describedBy.includes(id)){
if(!field.hasAttribute('data-orig-describedby')){
field.dataset.origDescribedby=describedBy;}
field.setAttribute('aria-describedby',describedBy?`${describedBy} ${id}`:id);}
const ref=field.parentNode.querySelector('.char-counter')
??field.parentNode.querySelector('.form-hint')
??field;
ref.after(el);
if(!field.hasAttribute('data-live-validate')){
field.dataset.liveValidate='';
const clear=()=>{if(field.checkValidity())clearFieldError(field);};
field.addEventListener('input',clear);
field.addEventListener('change',clear);}};
const clearFieldError=(field)=>{
const id=errorIdFor(field);
document.getElementById(id)?.remove();
field.removeAttribute('aria-invalid');
if(field.hasAttribute('data-orig-describedby')){
const original=field.dataset.origDescribedby;
if(original){
field.setAttribute('aria-describedby',original);}else{
field.removeAttribute('aria-describedby');}
delete field.dataset.origDescribedby;}};
const validateForm=(form)=>{
const fields=form.querySelectorAll('input, select, textarea');
let firstInvalid=null;
for(const field of fields){
if(field.type==='hidden'||field.disabled||field.name==='website')continue;
clearFieldError(field);
if(!field.checkValidity()){
showFieldError(field);
firstInvalid??=field;}}
firstInvalid?.focus();
return!firstInvalid;};
const trackDirty=(form)=>{
let dirty=false;
const warn=(e)=>{
if(!dirty)return;
e.preventDefault();};
const markDirty=(e)=>{
if(!e.isTrusted)return;
if(e.target.type==='file'){
dirty=e.target.files.length>0;}else{
dirty=true;}};
form.addEventListener('input',markDirty);
form.addEventListener('change',markDirty);
form.addEventListener('reset',()=>{dirty=false;});
form.addEventListener('submit',()=>{
dirty=false;
window.removeEventListener('beforeunload',warn);});
window.addEventListener('beforeunload',warn);};
const charCounter=(textarea,max)=>{
const counter=document.createElement('span');
counter.className='char-counter';
const counterId=`${textarea.id || 'textarea'}-counter`;
counter.id=counterId;
counter.setAttribute('aria-live','polite');
textarea.setAttribute('aria-describedby',counterId);
const update=()=>{
const len=textarea.value.length;
counter.textContent=`${len} / ${max}`;};
let debounceTimer;
textarea.addEventListener('input',()=>{
update();
clearTimeout(debounceTimer);
debounceTimer=setTimeout(()=>{
counter.setAttribute('aria-live','polite');},1_000);
counter.setAttribute('aria-live','off');});
update();
textarea.parentNode.insertBefore(counter,textarea.nextSibling);};
const imagePreview=(input,container)=>{
let objectUrl=null;
const allowed=(input.accept||'image/jpeg,image/png,image/webp')
.split(',')
.map((t)=>t.trim());
const cleanup=()=>{
if(objectUrl){
URL.revokeObjectURL(objectUrl);
objectUrl=null;}
container.textContent='';};
input.addEventListener('change',()=>{
cleanup();
const file=input.files?.[0];
if(!file)return;
if(!allowed.includes(file.type)){
toastManager.show('Invalid file type. Please choose a JPEG, PNG, or WebP image.','error');
input.value='';
return;}
objectUrl=URL.createObjectURL(file);
const img=document.createElement('img');
img.src=objectUrl;
img.alt='Image preview';
container.appendChild(img);});
input.form?.addEventListener('reset',cleanup);};
const initFormValidation=()=>{
document.addEventListener('submit',(e)=>{
const form=e.target;
if(!form.hasAttribute('novalidate'))return;
if(form.method?.toLowerCase()!=='post')return;
if(!validateForm(form))e.preventDefault();});};
const initServerErrorFocus=()=>{
const target=document.querySelector('form [aria-invalid="true"]:not([type="hidden"]):not([disabled])',);
if(target instanceof HTMLElement)target.focus({preventScroll:false});};
const initDoubleSubmitGuard=()=>{
const pendingButtons=new Set();
document.addEventListener('submit',(e)=>{
if(e.defaultPrevented)return;
const form=e.target;
if(form.method?.toLowerCase()!=='post')return;
const btn=e.submitter
??form.querySelector('button[type="submit"], input[type="submit"], button:not([type])');
if(!btn||btn.disabled)return;
btn.disabled=true;
pendingButtons.add(btn);
if(btn.tagName==='BUTTON'){
btn.dataset.originalLabel=btn.textContent;
btn.textContent='Submitting\u2026';}});
window.addEventListener('pageshow',(e)=>{
if(e.persisted&&document.querySelector('form[method="post"] input[name="csrf_token"]')){
location.reload();
return;}
for(const btn of pendingButtons){
btn.disabled=false;
if(btn.dataset.originalLabel){
btn.textContent=btn.dataset.originalLabel;
delete btn.dataset.originalLabel;}}
pendingButtons.clear();});};
const initPaginationScroll=()=>{
if(sessionStorage.getItem('nt-paginated')){
sessionStorage.removeItem('nt-paginated');
const main=document.getElementById('main-content');
main?.scrollIntoView({behavior:'smooth'});}
document.addEventListener('click',(e)=>{
const link=e.target.closest('.pagination a');
if(link)sessionStorage.setItem('nt-paginated','1');});};
const initToolSuggest=()=>{
for(const input of document.querySelectorAll('input[data-suggest="tools"]')){
if(!input.id)continue;
const availableOnly=input.hasAttribute('data-available-only');
AutosuggestController.create({
inputId:input.id,
fetchUrl:(q)=>`/tools/suggest?q=${encodeURIComponent(q)}${availableOnly ? '&available=1' : ''}`,
labelKey:'name',
onSelect:(item)=>{window.location.href=`/tools/${item.id}`;},});}};
const initAdminSuggest=()=>{
for(const input of document.querySelectorAll('input[data-suggest="admin"]')){
if(!input.id)continue;
const type=input.dataset.suggestType;
if(!type)continue;
const isGlobal=type==='all';
AutosuggestController.create({
inputId:input.id,
fetchUrl:(q)=>`/admin/suggest?type=${encodeURIComponent(type)}&q=${encodeURIComponent(q)}`,
labelKey:isGlobal?'name':null,
onSelect:isGlobal
?(item)=>{window.location.href=item.url;}:undefined,
renderItem:isGlobal
?(li,item)=>{
const badge=document.createElement('span');
badge.textContent=item.type;
badge.dataset.badge='';
li.append(item.name,' ',badge);}:undefined,});}};
class BadgeManager{#getBellLink(){
return document.querySelector('#bell-wrapper > a[href="/notifications"]');}
set(count){
const bellLink=this.#getBellLink();
if(!bellLink)return;
let badge=bellLink.querySelector('span');
if(count>0){
if(!badge){
badge=document.createElement('span');
bellLink.appendChild(badge);}
badge.textContent=count;
bellLink.setAttribute('aria-label',`Notifications (${count} unread)`);}else{
badge?.remove();
bellLink.setAttribute('aria-label','Notifications');}
const mobileBell=document.querySelector('[data-mobile-auth] a[href="/notifications"]');
if(mobileBell){
const icon=mobileBell.querySelector('i');
mobileBell.textContent='';
if(icon)mobileBell.appendChild(icon);
mobileBell.append(count>0?` Notifications (${count})`:' Notifications');}}
read(){
const badge=this.#getBellLink()?.querySelector('span');
return badge?parseInt(badge.textContent,10)||0:0;}}
const badgeManager=new BadgeManager();
window.NT={
fetch:ntFetch,
toast:(message,type,duration)=>toastManager.show(message,type,duration),
confirm:(message,confirmLabel)=>confirmDialog.show(message,confirmLabel),
decision:(data)=>decisionDialog.show(data),
autosuggest:(config)=>AutosuggestController.create(config),
sanitizeHtml:window.__ntSanitizeHtml,
trustedScript:window.__ntTrustedScript,
parseHtmlDocument:window.__ntParseHtmlDocument,
style:Object.freeze({
setRule:(key,selector,declarations)=>styleManager.setRule(key,selector,declarations),
removeRule:(key)=>styleManager.removeRule(key),}),
applyFocalPoints:(root)=>styleManager.applyFocalPoints(root),
bustImageCache:(img)=>{
const bust=`v=${Date.now()}`;
const update=(url)=>{
const[base]=url.split('?');
return`${base}?${bust}`;};
if(img.src)img.src=update(img.src);
if(img.srcset)img.srcset=img.srcset.replace(/(\S+\.(jpe?g|png|webp)\S*)/gi,(m)=>update(m));
const picture=img.closest('picture');
if(picture){
for(const source of picture.querySelectorAll('source[srcset]')){
source.srcset=source.srcset.replace(/(\S+\.(jpe?g|png|webp)\S*)/gi,(m)=>update(m));}}},
badge:Object.freeze({
set:(count)=>badgeManager.set(count),
read:()=>badgeManager.read(),}),
focus:Object.freeze({
trap:(container)=>focusManager.trap(container),
announce:(message,priority)=>focusManager.announce(message,priority),}),
form:Object.freeze({
trackDirty,
charCounter,
imagePreview,
validate:validateForm,}),};
delete window.__ntSanitizeHtml;
delete window.__ntTrustedScript;
delete window.__ntParseHtmlDocument;
document.addEventListener('DOMContentLoaded',()=>{
const decisionEl=document.getElementById('decision-data');
if(decisionEl){
try{
const data=JSON.parse(decisionEl.textContent);
const flashSibling=decisionEl.previousElementSibling;
decisionEl.remove();
flashSibling?.matches('[data-flash]')&&flashSibling.remove();
decisionDialog.show(data);}catch{}}
toastManager.migrateFlashMessages();
initPaginationScroll();
initFormValidation();
initServerErrorFocus();
initDoubleSubmitGuard();
styleManager.applyFocalPoints();
ScrollToTop.init();
LazyFadeIn.init();
initToolSuggest();
initAdminSuggest();});